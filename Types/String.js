"use strict";
(() => {
    var _a;
    const Batman = "Batman";
    const LinternaVerde = "Linterna Verde";
    const Superman = `Superman`;
    console.log(`I'm ${Batman}`);
    console.log(Batman.toLowerCase());
    console.log(((_a = Batman[10]) === null || _a === void 0 ? void 0 : _a.toLowerCase()) || 'there is not index asked');
})();
